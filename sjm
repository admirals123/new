#!/bin/bash

# Service name to search for
service_name="<service>"

# Replacement name
replacement_name="crs"

# Input YAML file
yaml_file="a.yaml"

# Use sed to replace the service name in the YAML file
sed -i "s/\b${service_name}\b/${replacement_name}/g" "$yaml_file"

echo "Replacement complete."

